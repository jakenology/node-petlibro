import PetLibroAPI from './index.js';

// Default export (allows: import PetLibroAPI from 'petlibro-api')
export default PetLibroAPI;

// Named export (allows: import { PetLibroAPI } from 'petlibro-api')
export { PetLibroAPI };
